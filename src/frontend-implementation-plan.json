{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Fix Mark Past Attendance calendar header layout/spacing",
  "requirements": [
    {
      "id": "REQ-115",
      "summary": "Adjust premium calendar CSS so the calendar header, weekday row, and date grid are clearly separated with a perfectly centered month title and no overlap at any viewport size.",
      "acceptanceCriteria": [
        "In the past attendance date picker (frontend/src/components/pastAttendance/MarkPastAttendanceSheet.tsx, Calendar with className=\"premium-calendar\"), the header renders as: left arrow | centered month+year | right arrow, with the month text visually centered between arrows.",
        "Header has horizontal padding of at least 16px on both sides and a bottom margin of at least 12px before the weekday row.",
        "Weekday row (SU MO TU WE TH FR SA) is visually separated below the header and never overlaps with the arrows or month label.",
        "Date grid is visually separated below the weekday row with clean padding/spacing; no elements overlap at any viewport width.",
        "Month title does not shift position when navigating months; arrows stay pinned to left/right extremes.",
        "Navigation arrow buttons have comfortable clickable/tappable area (8–12px padding equivalent) and do not collide with the weekday row.",
        "No calendar functionality changes: date selection, disabled future dates, and month navigation behavior remain unchanged."
      ],
      "file_operations": [
        {
          "path": "frontend/src/index.css",
          "operation": "modify",
          "description": "Update the .premium-calendar DayPicker layout styles to enforce three non-overlapping vertical sections (caption/navigation row, weekday row, date grid). Implement a robust header layout (e.g., grid-based caption row with fixed left/right nav areas and a centered label that doesn’t affect flow or collide), ensure >=16px horizontal padding and >=12px spacing below the header, add explicit spacing between weekday row and date grid, and increase nav button hit area without changing behavior. Use the shadcn/ui Calendar composition as-is (do not modify files under frontend/src/components/ui); Verify the component's usage instructions before implementing."
        }
      ]
    }
  ]
}